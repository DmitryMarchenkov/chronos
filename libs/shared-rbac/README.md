# shared-rbac

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-rbac` to build the library.

## Running unit tests

Run `nx test shared-rbac` to execute the unit tests via [Jest](https://jestjs.io).
