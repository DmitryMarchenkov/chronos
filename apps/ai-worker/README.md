# AI Worker (placeholder)

This is a minimal FastAPI placeholder for future AI background workers.

Local run (optional):

```
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python -m uvicorn main:app --reload --port 8001
```
