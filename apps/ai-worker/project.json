{
  "name": "ai-worker",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/ai-worker",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "python3 -m compileall apps/ai-worker"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "python3 -m uvicorn main:app --reload --port 8001",
        "cwd": "apps/ai-worker"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "python3 -m unittest",
        "cwd": "apps/ai-worker"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "python3 -m compileall main.py",
        "cwd": "apps/ai-worker"
      }
    }
  }
}
